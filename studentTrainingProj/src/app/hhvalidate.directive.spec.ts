import { HhvalidateDirective } from './hhvalidate.directive';

describe('HhvalidateDirective', () => {
  it('should create an instance', () => {
    const directive = new HhvalidateDirective();
    expect(directive).toBeTruthy();
  });
});
